{
  "name": "@nummi/core",
  "version": "0.1.0",
  "private": true,
  "description": "Mesin Nummi: ledger, invariant, split, rules, format. TypeScript murni, nol dependency framework.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
